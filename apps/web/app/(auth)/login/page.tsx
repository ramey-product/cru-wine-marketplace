import { LoginForm } from '@/components/features/auth/LoginForm'

export const metadata = {
  title: 'Sign In — Cru',
  description: 'Sign in to your Cru account to discover wines you\'ll love.',
}

export default function LoginPage() {
  return <LoginForm />
}
