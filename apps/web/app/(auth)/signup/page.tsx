import { SignupForm } from '@/components/features/auth/SignupForm'

export const metadata = {
  title: 'Create Account — Cru',
  description: 'Join Cru to discover wines from local retailers who know them best.',
}

export default function SignupPage() {
  return <SignupForm />
}
