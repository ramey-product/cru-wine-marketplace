/**
 * Supabase Database Types
 *
 * TODO: Generate real types by running:
 *   npx supabase gen types typescript --project-id <project-id> > types/database.ts
 *
 * This placeholder allows the codebase to compile. Once the Supabase project
 * is connected, regenerate this file to get full type-safety for all tables,
 * views, and RPC functions.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface Database {}
